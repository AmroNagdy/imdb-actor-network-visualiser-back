https://www.imdb.com/interfaces/

1. Download title.basics.tsv.gz and collect all tconst where titleType=='movie' as Set[String].

2. Download title.ratings.tsv.gz and collect top 1000 tconst where tconst in Set of tconst found in 1a.

3. Iterate back through title.basics.tsv.gz with the list of tconsts and averageRatings from 2 and build the JSON:

{
	tconst: String,
	averageRating: Double,
	primaryTitle: String,
	startYear: Int,
	isAdult: Boolean,
	runtimeMinutes: Double,
	genres: List[String]
}

4a. Download title.principals.tsv.gz and collect all tconst where tconst in Set of tconst found in 1a.
4b. For each tconst, collect all nconst where category=='actor' or category=='actress'.
4c. Append to JSON object in step 1c:

{
	nconsts: List[String], // Name IDs of actors in the film.
}

5a. Download name.basics.tsv.gz and map to JSON Object:

{
	nconst: String,
	primaryName: String,
	birthYear: Int,
	deathYear: Int
}

5b. Map the nconsts: List[String] in step 3c to a List of actor/actress JSON objects built in 4a.
